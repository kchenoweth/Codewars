//Opposite Number - 8kyu

//As simple as it gets, make whatever number is input the opposite, or negative.

function opposite(number) {
    return(-number)
}

//If you MUST do math, multiply the input by -1

function opposite(number) {
    return number * (-1);
}